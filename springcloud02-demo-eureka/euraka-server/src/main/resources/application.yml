#顶底eureka服务器的端口
server:
  port: 8888
#声明eureka微服务的名称
spring:
  application:
    name: eureka-server

eureka:
 client:
  register-with-eureka: false
  fetch-registry: false
  service-url:
    defaultZone: http://192.168.1.112:${server.port}/eureka
